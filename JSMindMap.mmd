Mind Map generated by NB MindMap plugin   
> __version__=`1.1`,showJumps=`true`
---

# JavaScript

## language

### 数据类型

#### 数组\[Array\]

##### 方法
- LINK
<pre>https://zh.javascript.info/array-methods</pre>

###### 添加/删除元素

####### push\(\.\.\.items\)
- NOTE
<pre>向尾端添加元素</pre>

####### pop\(\)
- NOTE
<pre>从尾端提取一个元素</pre>

####### shift\(\)
- NOTE
<pre>从首端提取一个元素</pre>

####### unshift\(\.\.\.items\)
- NOTE
<pre>向首端添加元素</pre>

####### splice\(pos, deleteCount, \.\.\.items\)
- NOTE
<pre>从 pos 开始删除 deleteCount 个元素，并插入 items</pre>

####### slice\(start, end\)
- NOTE
<pre>创建一个新数组，将从索引 start 到索引 end（但不包括 end）的元素复制进去</pre>

####### concat\(\.\.\.items\)
- NOTE
<pre>返回一个新数组：复制当前数组的所有元素，并向其中添加 items。如果 items 中的任意一项是一个数组，那么就取其元素</pre>
